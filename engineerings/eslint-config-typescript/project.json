{
  "tags": ["type:engineering"],
  "namedInputs": {
    "default": ["{projectRoot}/**/*"]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "outputs": ["dist"],
      "dependsOn": ["^build"],
      "options": {
        "cwd": "engineerings/eslint-config-typescript",
        "commands": ["pnpm exec tsx ./scripts/build.ts"],
        "parallel": false
      }
    }
  }
}
